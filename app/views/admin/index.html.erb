<div class="row">
	<div class="col-md-3">
		<%= link_to 'Nuovo Login', new_admin_path, class: "btn btn-primary" %>
	</div>	
</div>

<br>

<div class="row">
	<div class="col-md-12">
		<table class="table table-striped table-bordered table-hover" id="dataTables-utenti">
		<thead>
		  <tr>
				
				<th><i class="material-icons">drafts</i>Email (login)</th>
				<th>Privilegi</th>
				<th>Azioni</th>
		  </tr>
	  </thead>
	 
	 	<tbody>
		  <% @utenti.each do |u| %>
				<tr>
					<td><%= u.email %></td>
					<td></td>
					<td><%= link_to 'modifica password', edit_admin_path(u.id) %></td>
				</tr>
		  <% end %>
	  </tbody>
	</table>

	</div>
</div>
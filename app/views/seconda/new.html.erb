<div class="row">
	<div class="col-md-12">
		<h1>Creazione Seconda rilevazione per il seggio <%= @seggio.numero %></h1>
	</div>
</div>

<%= render 'form', {url: seggi_seconda_index_path, method: 'nessuno'} %>

<%= debug(params) %>
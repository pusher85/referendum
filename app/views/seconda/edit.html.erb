<div class="row">
	<div class="col-md-12">
		<h1>Modifica Seconda rilevazione per il seggio <%= @seggio.numero %></h1>
	</div>
</div>

<%= render 'form', {url: seggi_seconda_path, method: 'patch'} %>

<%= debug(params) %>